/*
const initialState = {
  mails: []
}

function mailApp(state = initialState, action) {
    switch (action.type) {
        case ADD_MAIL:
          return Object.assign({}, state, {
            visibilityFilter: action.filter
          })
        case ADD_TODO:

  return state
    }*/